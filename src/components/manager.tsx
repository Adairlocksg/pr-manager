
export default function Manager(/* { activeContent }: ManagerProps */) {
 /*  const renderContent = () => {
    switch (activeContent) {
      case "home":
        return <h2>Welcome to the Dashboard</h2>;
      case "users":
        return <h2>User Management</h2>;
      case "settings":
        return <h2>Settings</h2>;
      default:
        return <h2>Select an option from the sidebar</h2>;
    }
  }; */

  return <div className="flex-1 p-8">asdasdsa</div>;
}
